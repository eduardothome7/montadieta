<%= form_with(model: diet, local: true) do |form| %>
  <% if diet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(diet.errors.count, "error") %> prohibited this diet from being saved:</h2>

      <ul>
      <% diet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :objective %>
    <%= form.text_field :objective, id: :diet_objective %>
  </div>

  <div class="field">
    <%= form.label :tmb %>
    <%= form.number_field :tmb, id: :diet_tmb %>
  </div>

  <div class="field">
    <%= form.label :kcal %>
    <%= form.number_field :kcal, id: :diet_kcal %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
