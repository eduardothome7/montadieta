<%= form_with(model: food, local: true) do |form| %>
  <% if food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(food.errors.count, "error") %> prohibited this food from being saved:</h2>

      <ul>
      <% food.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :name, id: :food_name %>
  </div>

  <div class="field">
    <%= form.label :Calorias %>
    <%= form.number_field :kcal, id: :food_kcal %>
  </div>

  <div class="field">
    <%= form.label :carboidratos %>
    <%= form.number_field :carb, id: :food_carb %>
  </div>

  <div class="field">
    <%= form.label :proteína %>
    <%= form.number_field :protein, id: :food_protein %>
  </div>

  <div class="field">
    <%= form.label :Gorduras %>
    <%= form.number_field :fat, id: :food_fat %>
  </div>

  <div class="field">
    <%= form.label :Unidade %>
    <%= form.check_box :is_unity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
