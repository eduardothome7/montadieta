<%= form_with(model: category_food, local: true) do |form| %>
  <% if category_food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category_food.errors.count, "error") %> prohibited this category_food from being saved:</h2>

      <ul>
      <% category_food.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :category_food_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :category_food_description %>
  </div>

  <div class="field">
    <%= form.label :icon %>
    <%= form.text_field :icon, id: :category_food_icon %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
